# Staged question captures

These are captured question images that are waiting for their answer point to
be marked on the master map. Open **Edit answers** on the RooGuessr website,
choose a capture, place its pin, and copy the generated coordinate line into
the location's `answer.txt` file.
