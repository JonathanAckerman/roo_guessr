# RooGuessr TODO

- Give every location two user-authored 7:5 images: `question.webp`, the zoomed-in view shown before the guess, and `answer.webp`, a higher-Z view shown after the guess to reveal the location in context. This includes UI to add it to the zip that the user can download from the editor, and when the player guesses we need to add the answer image somewhere so they can see
- display an error message if they try to use an image that is too small
- Add a slider from 5 to Max images for how many questions to offer the player, default to 10.