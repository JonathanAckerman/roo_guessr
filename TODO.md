# RooGuessr TODO

All currently tracked features are implemented.

> Migration note: the 20 locations created before answer-image support still
> fall back to `question.webp` after a guess. They are temporarily exempt from
> the `answer.webp` requirement until contextual answer images are authored.
